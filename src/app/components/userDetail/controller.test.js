import UserDetail from './controller';

describe('UserDetail controller', () => {
    let ctrl;

    beforeEach(() => {
        ctrl = new UserDetail();
    });

    describe('$onInit', () => {
        //...
    });

    //..
});
